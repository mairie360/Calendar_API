[alias]

#This alias check the lint
lint_check = "fmt --all -- --check"

# This alias formats the code
lint_fix = "fmt --all"

# This alias check the code
check_code = "clippy --all-targets --all-features -- -D warnings"

# This alias generate the OpenAPI documentation
open_api = "run --example generate_openapi"

cov_test = ["llvm-cov", "--workspace", "--ignore-filename-regex", "endpoints|main\\.rs|lib\\.rs", "--fail-under-lines", "60"]

cov = ["llvm-cov", "--workspace", "--ignore-filename-regex", "endpoints|main\\.rs|lib\\.rs", "--fail-under-lines", "60", "--codecov", "--output-path", "codecov.json"]

# Les images Postgres/Liquibase des tests d'intégration (mairie360_api_lib::test_setup) doivent inclure
# le changeset v1.2.0 du schéma : catégorie, service et lieu des événements, jours des récurrences. Il est
# livré depuis la release 1.1.0 des images ; la version utilisée ici doit rester alignée sur les fichiers docker-compose.
[env]
TEST_DB_VERSION = "1.2.0"
